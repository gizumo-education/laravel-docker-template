1. view関数の引数（空欄部分の補完）
ここ、大事なので埋めておきましょう！

第1引数： 表示したいBladeファイルの名前（例: 'todo.index'）。resources/views/ からのパスをドット区切りで指定。

第2引数： Bladeに渡したいデータ（連想配列）。

何をしているか： 「指定した型紙（Blade）に、指定したデータ（変数）を流し込んで、完成したHTMLをブラウザに返す」処理。

2. Requestクラスのallメソッド
補足： 認識通りですが、実務では all() よりも only() や input() を使うことが多いです。なぜなら、all() は _token（CSRFトークン）などの不要なデータまで全部拾ってしまうからです。

3. fillメソッドと$fillable（重要！）
補足： 非常に良い説明です！付け加えるなら、**「Modelクラスで $fillable を定義していないと、fill() メソッドを使ってもデータは代入されない（無視される）」**という点も覚えておくと完璧です。

4. {{ }} とは何の省略系か
修正： <?php echo ...; ?> の省略形…だけではありません！

最重要ポイント： Laravelの {{ }} は、自動的に 「XSS対策（サニタイジング）」 をしてくれます。

つまり、悪意のあるJavaScriptなどが含まれていても、ただの文字列として表示してくれる安全な書き方です。

最強の**「逆引きレビュー対策シート」**にまとめました。これをコピーして自分宛に送るか、メモ帳に貼っておけば安心です。
- 🚀 Laravel 
1. データベース・モデル編質問内容模範解答のポイントall() で実行されるSQLは？SELECT * FROM todos; 
- 全件取得します。all() の返り値は？Collection インスタンス。配列より多機能な「Laravel専用配列」
- ORM（Eloquent）の利点は？SQLを直接書かず、PHPのクラス操作だけでDBを扱えること。保守性が上がる。
- Migrationのメリットは？DBの構造をコードで管理できる点。
**「誰でも・どこでも・同じ構成」**を再現（migrate）できます。Seederの役割は？開発用のテストデータを一括で流し込むための仕組みです。
2. 画面表示・テンプレート編質問内容模範解答のポイントview() の引数は？第1：Bladeファイル名、第2：渡すデータ（連想配列）です。{{ }} の役割は？echo の省略形。かつ、**XSS攻撃を防ぐサニタイジング（無害化）**を自動で行います。
- @extends と @yield は？extends は親（枠組み）の指定。yield は親にある「空き地（予約席）」です。@section は？子Bladeで書きたい中身を定義し、親の @yield に注入します。
3. データ作成・セキュリティ編質問内容模範解答のポイント@csrf はなぜ必要？**「偽造されたリクエスト（CSRF攻撃）」**を防ぐため。正規のフォームからの送信だと証明します。$fillable を書く理由は？**「一括代入の脆弱性」**対策。意図しないカラム（管理者フラグ等）を書き換えられないよう制限します。store 後のリダイレクトは？保存後に画面を再読み込みした際の**「二重送信（二重保存）」を防ぐため**に必須です。route() 関数を使う利点は？URLを直接書かず「名前」で管理するため、URL変更時の修正が1箇所で済む（保守性向上）からです。

# section３
1. CRUD	機能について
CREATE	データベースにデータを新規追加する
READ	データベースからデータを取得・表示する
UPDATE	データベースの既存データを更新する
DELETE	データベースからデータを削除する
- データを扱う上で4つの基本的な操作
 多くのアプリケーションはCRUD機能で成り立っている

1. MVC構成
- 開発効率を高めるために作られたアーキテクチャ
  再利用性・可読性を優れ、保守性を高めてくれる
  MVCごとに役割が分担されており、役割を理解することが重要である

- Model (モデル)……DBとのやり取りを司る
- View (ビュー)……ユーザ (Client) に表示する画面の生成を司る
- Controller (コントローラ)……ユーザからの入力 (送信データ) に基づき、 Model・Viewの制御を司る
- Routing	ユーザからの依頼（リクエスト）に応じてどの処理を動かすかの判断を司る

# section4：
## MigrationとSeeder
- Migration (マイグレーション) : テーブルの作成を担う
‐ 使用する際は下記を使用：
実行にはphp artisan migrate
元に戻すにはphp artisan migrate:rollback

- Seeder (シーダー) : レコードの作成を担う

###　なせマイグレーションが必要か？
- SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要。マイグレーションファイルをGitで共有することで、開発者全員が同じテーブルを作成することができる
→どういうことか？
チームでアプリケーションを開発する際に、開発者同士のテーブル構成は全く同じにする必要があるが、テーブル数が増えると実行と管理が手間になる。
今回使用したマイグレーションファイルを開発者同士でgitなどで共有しておけばマイグレーションを実行するだけで、開発者同士のテーブル構成を統一させることができる。同じコードを書いたり大事なデータを消したり事故が防げる。

#　section5：テストデータの導入 〜Seeder〜
- シーダーを使ってテストデータを投入する手順
1.シーダーファイルの作成……artisanで作成
→atabases/seeds配下にTodoSeeder.phpファイルができる。
- データベースに初期データやテスト用データの運用
src/database/seeds/TodoSeeder.phpの構成クラス
- TodoSeederクラスとrun()メソッドで構成されている。

- 2.シーダーファイルに作成したいテストデータ情報を記載
- run()メソッド
→テストデータを投入する実装が必要になる。
連想配列の形で用意する。
- ここで重要なのがDB::table('todos')
→tableメソッドの引数のテーブルを操作するための準備
- insertメソッド
→引数のデータをテーブルに投入するINSERT文をまとめて実行する
- ★DB::table('todos')->truncate();の必要性
テーブルを一旦空にしてからデータを入れるため。
これがないとシーダーを何度も実行しても同じ初期状態に戻せない。同じデータがどんどん追加されていく
- 役割
- todos テーブルの 全レコードを削除
- ID のオートインクリメントもリセット
これによりシーダーを実行するたびに 常に同じ状態からデータを作れる

- 3.シーダークラスを登録
- database/seeds/DatabaseSeeder.phpのcallメソッドに登録することで実行することができる
  public function run()
    {
        $this->call([
            TodoSeeder::class,
        ]);
    }

- 4.シーダーを実行
- artisanコマンドを用いることでDatabaseSeeder.phpのcallメソッドを実行することができる。

# DBに保存されているデータを画面に表示するために実装しなければいけない項目
1. 一覧画面のURIの設定
1. 一覧画面を表示
1. DBに登録されているデータを取得する
1. 取得したデータを画面に渡す
1. 画面上にデータを表示する
- phpとLaravelの違い
- PHP Appレッスン：直前のURIごとに実行する処理を変更する関数を定義
- Laravel：URIとHTTPメソッドの組み合わせで実行する処理を変更することができる。
- ★ルート定義★……URIとHTTPメソッドの組み合わせで実行する処理を指定すること
- 例：routes/web.php

// 追加
Route::get('/todo', function () {
    echo 'Hello World!';
});
- 第一引数：/todo'→ルートのURLパス
- 第二引数（Second argument）
→ ルートにアクセスしたときに実行される処理（クロージャ／コールバック）
- 余談[get/postの違い]
メソッド	用途
- Route::get()	GETリクエストを処理（ブラウザでURLにアクセスしたときなど）
- Route::post()	POSTリクエストを処理（フォーム送信やAPIでのデータ作成など）
# section７　ToDo一覧表示機能の作成 〜一覧画面を表示〜
- Controllerで処理を扱うこと
→保守性の向上。
- 1️⃣ ルートと処理を分ける
routes/web.php に処理を書きすぎると ファイルが長くなり、何をしているか分かりにくい
コントローラーに処理をまとめると ルートはURLとコントローラーの紐付けだけ になる
- 2️⃣ 再利用性が上がる
同じ処理を別ルートやAPIで呼びたいとき、コントローラーにまとめておけば 何度でも使える
routes/web.php に直接書くと、コピー＆ペーストになって非効率
- 3️⃣ テストがしやすい
コントローラーのメソッドなら PHPUnitで単体テスト可能
routes/web.php に直接処理を書くと テストしにくい
- 4️⃣ 大規模プロジェクトでの保守
プロジェクトが大きくなると routes/web.php に処理が増えすぎて 誰も読めなくなる
コントローラーにまとめると、役割ごとにファイルが分かれて管理しやすい

図書館の本棚をイメージ。本ごとの分類によって棚をわけるように、処理ごとを分類するイメージ。
例え：
- routes/web.php は 館内マップ
- 「このURL（入口）に行ったらどの棚（Controller）に行けばいいか」だけ書く
- 処理の中身は書かない
- Controller は 処理ごとにファイルやメソッドで分類された棚
- TodoController → Todoの棚

- UserController → ユーザーの棚

- 処理ごとに分かれているので探しやすい

- View は 本のページ
実際に画面に表示される内容

実際のコード参考
館内マップ（routes/web.php）
└ /todo → TodoController@index  ← Todo棚の場所を示す
└ /user → UserController@index  ← User棚の場所を示す

棚（Controller）
TodoController
├ index()   ← 本棚の本1：Todo一覧を表示
├ create()  ← 本棚の本2：Todo作成
UserController
├ index()   ← 本棚の本1：ユーザー一覧

## 実際にControllerで処理を扱うには？
- Route::get()の第二引数に対象のControllerとそのメソッドを指定する必要がある。
今回はTodoControllerのindexメソッドに処理を移譲する。
- php artisan make:controller TodoController
- これにより、php artisan make:controller TodoControllerが作成される。
- routes/web.php
- Route::get('/todo', 'TodoController@index');
第一引数：todoのURL、第二引数：TodoController@indexの作成
