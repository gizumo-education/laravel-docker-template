# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT * FROM todos

### Todoモデルのallメソッドの返り値は何か
Illuminate\Database\Eloquent\Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
・コードが読みやすい
・用意された様々なメソッドが利用できる

### view関数の第1・第2引数の指定と何をしているか
（view関数は、Laravelでのみ使用できるヘルパ関数と呼ばれる関数の一種）
第1引数は画面に表示したいbladeファイル、第2引数は渡したいデータ（連想配列の形）

### index.blade.phpの$todos・$todoに代入されているものは何か
$todos todosテーブルの全レコードを取得したコレクション
$todo　foreachにより$todosから1つずつ順番に抜き出したもの

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
現在のHTTPリクエストから全ての入力データを取得している

### fillメソッドは何をしているか
渡された連想配列を、指定のモデルの中の対応する場所に格納している

・fillメソッドは、連想配列の形式でデータを渡される事で、対応する複数のカラムのそれぞれに値を格納する事が可能。
・「複数代入」というコードにより、悪意のあるユーザーがブラウザの機能を使って入力項目を不正に増やした場合、本来であればユーザーが操作できない値までもが更新されてしまう可能性がある。
fillableは入力されたものをチェックする事で、そういった攻撃を防ぐ事が可能。
・saveメソッドを用いる事で、特定のテーブルに新しいレコードを保存できる。

### $fillableは何のために設定しているか
fillメソッドによって代入される部分を指定する事で脆弱性を緩和し、悪意のある攻撃を防ぐため

### saveメソッドで実行しているSQLは何か
INSERT INTO todos (user_id, content, created_at, updated_at)
VALUES (?, ?, ?, ?)

user_id			：コントローラーのコードで手動セット
content			：フォーム入力をfill()でセット
created_at / updated_at	：Eloquentの自動タイムスタンプ管理

### redirect()->route()は何をしているか
todo.index という名前がついたルートが示すURLに遷移する

「パスが/todoとなるURLにアクセスがあったら、TodoControllerのindexメソッドを実行する」
これを行う一連の流れtodo.indexという名前を付ける。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
→データベースの設計をコードとして書いて管理するメリットとは何か？

・SQLを知らずとも、PHPコードでテーブル操作ができるため学習コストが不要
・マイグレーションファイルをGitで共有することで、開発者同士のテーブル構成の統一が可能

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
・up()→php artisan migrate
　マイグレーションを実行し、新しいテーブル、カラム、またはインデックスを追加するために使用
・down()→php artisan migrate:rollback
　マイグレーションを元に戻すため、upメソッドによって実行する操作と逆の操作を実装し、以前の状態へ戻すべく使用

### Seederクラスの役割は何か
データベースに初期データやテスト用のダミーデータを自動で登録する。
Seederクラスを使用するメリットは以下の通り
・SQLを知らずとも、PHPコードでデータ登録ができるため学習コストが不要
・シーダーファイルをgit管理することで、テストデータを開発者間で共有できる
・php artisan db:seedコマンドで簡単にデータを一括挿入できる

### route関数の引数・返り値・使用するメリット
（route関数も、Laravelでのみ使用できるヘルパ関数と呼ばれる関数の一種）
・引数			→ルート名
・返り値		→指定したルート名に対応するURL文字列
・使用するメリット	 →・ルートのURLを直接書かずに済むため、後からURLを変更した場合にも対応が容易
			・名前でルートを管理する事でコードの可読性が向上

### @extends・@section・@yieldの関係性とbladeを分割するメリット
・@extends	子側に記述し、親のBladeテンプレートを指定する。
・@section	子側に記述し、親側の@yield部分で表示する内容を指定する。
・@yield	親側に記述し、子側の@section部分の内のどれを表示するかを指定する。

メリット：重複するコードを共通化して再利用できるようになるため、保守性が向上する

### @csrfは何のための記述か
フォームに隠しトークンを埋め込み、リクエストが正当なものかをサーバー側で検証できるようにする事により、CSRF攻撃を対策するため

### {{ }}とは何の省略系か
{{ }}＝<?php echo htmlspecialchars(); ?> 
htmlspecialchars()関数を使用してHTML特殊文字をエスケープしてくれるため、XSS攻撃を防止できる