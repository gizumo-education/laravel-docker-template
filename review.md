### Q1. `Todo`モデルの`all()`メソッドで実行しているSQLは何か？

**自分の回答：**`Todo::all()`は、**todosテーブルの全レコードを取得する**処理。関数の実行で`SELECT * FROM todos;`のSQLが実行される。つまり回答は`SELECT`

---

### Q2. `Todo`モデルの`all()`メソッドの返り値は何か？

**自分の回答：**EloquentのCollection

`Illuminate\Database\Eloquent\Collection`

配列(データ) + それを操作するメソッド群、をワンセットにしたオブジェクトがコレクション

```jsx
↓配列の場合
$numbers = [1, 2, 3, 4, 5];
$result = array_map(function ($n) {
    return $n * 2;
}, array_filter($numbers, function ($n) {
    return $n % 2 === 0;
}));

↓コレクションの場合
$result = collect([1, 2, 3, 4, 5])
->filter(fn($n) => $n % 2 === 0)
->map(fn($n) => $n * 2);
```

<aside>
💡

**処理の流れ**

Todo::all()
↓
SQLを実行
↓
DBからデータ取得
↓
Todoモデルの集合
↓
Collectionとして返る

</aside>

---

### Q3. 配列の代わりに`Collection`クラスを使用するメリットは？

**自分の回答：**配列の場合よりもデータを扱うための便利な機能が豊富なため可読性や保守性を上げるため

---

## 🔹 Viewへのデータ受け渡し

### Q4. `view()`関数の第1・第2引数には何を指定するか？また、何をしているか？

**自分の回答：第一引数は表示するBladeファイルを指定、第二引数はBladeに渡すデータ**

第一引数：'todos.index’

第二引数：[
'todos' => $todos
]

<aside>
💡

処理の流れ

Controller
↓
view()
↓
Bladeファイル

</aside>

---

### Q5. `index.blade.php`の`$todos`・`$todo`には、それぞれ何が代入されているか？

**自分の回答：複数のtodoを持つCollectionが$todos
1つ1つ取り出したものが$todo**

**$todosの中にTodo①②③（$todo）が入っているイメージ**

---

# ② Todo作成機能 ✏️

## 🔹 Request・Mass Assignment

### Q6. `Request`クラスの`all()`メソッドは何をしているか？

**自分の回答：**HTTPリクエストに含まれている入力データをすべて取得する

---

### Q7. `fill()`メソッドは何をしているか？

**自分の回答：**取得したデータをモデルに一括で代入するための目メソッド

| 処理 | 役割 |
| --- | --- |
| `$request->all()` | リクエストデータを取得 |
| `$todo->fill()` | モデルにデータを一括代入 |
| `$todo->save()` | DBに保存 |

---

### Q8. `$fillable`は何のために設定しているか？

**自分の回答：**一括代入で代入を許可するカラムを指定するため

---

## 🔹 データ保存・リダイレクト

### Q9. `save()`メソッドで実行しているSQLは何か？

**自分の回答：**新規ならinsert、既存ならupdate

---

### Q10. `redirect()->route()`は何をしているか？

**自分の回答：**一覧ページに戻る（リダイレクトする）

---

# ③ その他 🔧

## 🔹 Migration

### Q11. テーブル構成をマイグレーションファイルで管理するメリットは？

**自分の回答：SQLを介さなくても、直感的にファイル内で作業ができるので開発者間での認識齟齬がなくなる。データベースの構造変更をコードとして管理できること**

- Gitで管理できる
- チームで共有できる
- 同じDB構造を再現できる
- 変更履歴を管理できる
- rollbackで戻せる

---

### Q12. マイグレーションファイルの`up()`・`down()`は、何のコマンドを実行したときに呼び出されるか？

**自分の回答：**テーブルを作ったり変更する際、php artisan migrateを実行した際に呼び出されるが、upで行った変更を元に戻すときに呼び出されるのがdown()

---

## 🔹 Seeder

### Q13. Seederクラスの役割は何か？

**自分の回答：フレームワークでデータベースに初期データやテストデータを挿入するためのクラス**

テーブルの構造を作るのがmigration、テーブルにデータを入れるのがSeeder

---

## 🔹 Route

### Q14. `route()`関数の引数・返り値・使用するメリットは？

**自分の回答：処理がシンプルになって可読性が上がるし、URLを変更した際にも便利。保守性があがる**

第一引数がルート名で、第二引数に必要に応じてパラメータを渡せる。返り値はURLの文字列

---

## 🔹 Blade

### Q15. `@extends`・`@section`・`@yield`の関係性は？

**自分の回答：**

親の指定：`@extends`
入れる内容：`@section`
入れる場所：`@yield`

---

### Q16. Bladeファイルを分割するメリットは？

**自分の回答：**親ファイルと子ファイルに分けることで可読性が上がるのと、重複するコードが減るので保守性が上がる

共通部分は一箇所で管理することができる

---

### Q17. `@csrf`は何のための記述か？

**自分の回答：**`@csrf`は、**CSRF攻撃を防ぐためのCSRFトークンをフォームに埋め込む**ための記述

---

### Q18. `{{ }}`とは何の省略系か？

**自分の回答：**PHPの値をHTML出力するための記法で<?php echo $todo->content; ?>の省略形