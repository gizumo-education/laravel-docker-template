# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT文を実行している　SELECT * FROM todos
テーブル内の全レコードを取得する

### Todoモデルのallメソッドの返り値は何か
Illuminate\Database\Eloquent\Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
様々なメソッドに対応しデータ操作を行うことができる
配列だとループや条件文を使用し複雑になってしまう

### view関数の第1・第2引数の指定と何をしているか
第1引数…画面に表示させたいbladeファイルを指定
第2引数…渡したいデータ（連想配列の形で渡す）

view関数ではControllerからHTMLを表示（画面に表示）するbladeファイルにデータを渡すために使用している

### index.blade.phpの$todos・$todoに代入されているものは何か
$todos…Collectionクラスのインスタンス
$todo…Todoモデルのインスタンスのデータを1件ずつ取得し格納されている


## Todo作成機能

### Requestクラスのallメソッドは何をしているか
フォームから送信された値を一括で取得している

### fillメソッドは何をしているか
Todoインスタンスの各プロパティに保存したい値を一括で代入

### $fillableは何のために設定しているか
一括代入には脆弱性があるため悪意のあるユーザから攻撃されないようにするために設定をしている

### saveメソッドで実行しているSQLは何か
INSERT文を実行
データをデータベースに追加

### redirect()->route()は何をしているか
新規作成の処理が完了したら、一覧画面にリダイレクトを行う処理
route()の引数には、ルート先（リダイレクトする先・処理後に表示したいファイル）を設定

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
マイグレーションファイルをgitで共有することで開発者全員が同じテーブルを作成することが出きる
開発者同士でテーブル攻勢を統一することができる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()…artisanコマンド　php artisan migrate　※upメソッドではデータベースに新しいテーブルやカラム、インデックスを追加するために使用
マイグレーションファイルはupメソッドに記載の内容をもとに作成される

down()…rollbackコマンド　php artisan migrate:rollback　※upメソッドとは逆の操作で、データベースからテーブルやカラム、インデックスを削除される


### Seederクラスの役割は何か
データベースに初期データやテストデータを挿入する

### route関数の引数・返り値・使用するメリット
引数…ルート名、パラメータ
返り値…URLの文字列
メリット…URLが変更になっても、ルート名が変わらなければ修正箇所web.phpの一箇所のみになる　
そのため保守性が上がる

### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extends…bladeファイルを子に継承する親blade
@section…子ファイルで親の継承した内容に値を記載
@yield…子ファイルに記載した内容を親bladeファイルの項目に紐づけをする

メリット…重複するコードを共通化することによって保守性が上がる

### @csrfは何のための記述か
CSRF攻撃を防ぐための記述
トークンの発行を行う

### {{ }}とは何の省略系か
PHPのhtmlspecialchars関数の省略系
