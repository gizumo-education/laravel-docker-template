# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か

- todoテーブルのデータを全件取得するSQL（SELECT *）

### Todoモデルのallメソッドの返り値は何か

- テーブルのデータが連想配列の形で格納されたCollectionオブジェクト

### 配列の代わりにCollectionクラスを使用するメリットは

- チェーンメソッドが使用できる点。

### view関数の第1・第2引数の指定と何をしているか

- 第一引数: 表示するファイルの指定
- 第二引数: 第一引数で指定したファイルで表示したいデータの指定 (['blade内での変数名','渡すデータの値'])

### index.blade.phpの$todos・$todoに代入されているものは何か

- $todo: Todoクラスのオブジェクト
- $todos: テーブルのすべてのレコードのデータ

## Todo作成機能

### Requestクラスのallメソッドは何をしているか

- 入力値を全て取得している。

### fillメソッドは何をしているか

- 入力された値を$fillableで指定したカラムに、それぞれのプロパティ（カラムごとに）一括で代入している。

### $fillableは何のために設定しているか

- 一括代入を行うカラムを指定することで、ほかの項目に関する不正な入力の取得を阻止するため。

### saveメソッドで実行しているSQLは何か

- 入力されたコンテンツの値のテーブルに保存するSQL（INSERT）

### redirect()->route()は何をしているか

- データ保存後にリダイレクトするルートの指示。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット

- SQLを知らない人でもテーブルの操作が可能
- マイグレーションファイルをチーム間で共有することで、同じ構成のテーブルを作成できる。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか

- up: create(新しいテーブルやカラムなどの作成を実行したとき)
- down: rollback(テーブルやカラムなどの削除を行ったとき)

### Seederクラスの役割は何か

- テストデータを投入する役割

### route関数の引数・返り値・使用するメリット

- 引数: (URI,Controllerのメソッド)
- 返値: 第二引数で指定したメソッドの処理結果
- メリット
  - 使い回しがしやすい
  - URIの変更しやすい
  - HTTPリクエストの方法を指定できる 

### @extends・@section・@yieldの関係性とbladeを分割するメリット

- メリット
  - 共有部分を再利用できる。
  - @extends
    - 共有部分を子とするファイルで継承する。
  - @section 
    - 親とするファイルとの差分を指定。
  - @yeild
    - 差分（@section部分）の挿入。     

### @csrfは何のための記述か

- csrf対策として、トークンを設定するための記述

### {{ }}とは何の省略系か

- <?php?>
- echo
- htmlspecialchars()

