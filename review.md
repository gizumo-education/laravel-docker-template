# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
todoテーブルを選択するSELECT文
### Todoモデルのallメソッドの返り値は何か
Todoインスタンスが格納されているCollectionインスタンス
### 配列の代わりにCollectionクラスを使用するメリットは
Collectionクラスには、よく使う処理があらかじめメソッドとして用意されているため、
処理の意図がコードから直接読み取れ、可読性が高いのがメリット。
### view関数の第1・第2引数の指定と何をしているか
第一引数は、表示する画面。第二引数は、渡すデータ。
つまり、どの画面にどのデータを渡すかを決めている関数。
### index.blade.phpの$todos・$todoに代入されているものは何か
$todosは、Todoモデルのインスタンスが複数入ったCollection
$todoは、Todoモデルの１件分のインスタンス
## Todo作成機能

### Requestクラスのallメソッドは何をしているか
ユーザーから送られてきたデータを全て取得している
### fillメソッドは何をしているか
モデルのプロパティに配列でデータを一括で代入する
### $fillableは何のために設定しているか
フォームからユーザーIDなどを悪意のある書き換えをされないため、contentのみ受け取るなど制限をかけられる
### saveメソッドで実行しているSQLは何か
新しくcontentを追加するINSET文
### redirect()->route()は何をしているか
指定したルート名のURLへ移動してくださいという命令を出している。
新規作成の場合、一覧表示画面に戻る。
## その他

### テーブル構成をマイグレーションファイルで管理するメリット
DBを直接触らないため、環境が違う誰のPCでも同じDB構成が作成でき、失敗した時にやり直せる
### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()は、php artisan migrate
down()は、php artisan migrate:rollback
### Seederクラスの役割は何か
Seederクラスでテーブルにテストデータを入れることができ、
本番に向けてデータがある前提で開発を進めることができる
### route関数の引数・返り値・使用するメリット
第一引数はルート名、第二引数はURLパラメータ
返り値はURL文字列
メリットはURLを覚えなくていいこと
### @extends・@section・@yieldの関係性とbladeを分割するメリット
子ページで@extendsで共通部分を記述している親ページを指定して、@sectionの中にオリジナル部分を記述する。
親ページでこページのオリジナル部分に@yieldを挿入すると共通レイアウトとオリジナルの中身で分けることができる。
そのため、同じHTMLを記述せずに済み、読みやすくなる
### @csrfは何のための記述か
フォームを送信されたときにトークンを発行できるように@csrfと記述することで、CSRF対策ができる。
### {{ }}とは何の省略系か
出力とエスケープ処理ができるecho e()の省略系
