# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
todosテーブルのレコードを全件取得するSQLが実行されている。
SQL文：
SELECT "todos".* FROM "todos"

### Todoモデルのallメソッドの返り値は何か
Illuminate\Database\Eloquent\Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
Collectionインスタンスの中に5つのTodoインスタンスが格納されていて、そのTodoインスタンスはtodosテーブルのレコード一つずつに対応しているため、レコード情報をTodoインスタンスという形で取得でき、ただの配列として扱うよりも、各レコードのデータ（カラム）へのアクセスや操作が直感的で安全に行える点。
また、それらがCollectionに格納されていることで、配列操作に特化した便利な機能をそのまま利用できる点。

### view関数の第1・第2引数の指定と何をしているか
画面に表示したいファイルを第一引数で指定し、第二引数に渡したいデータを連想配列の形で渡すことができる。
view関数を用いることでControllerから指定したファイルへデータを渡すことができる。

### index.blade.phpの$todos・$todoに代入されているものは何か
$todosには、Controllerにて取得したCollectionインスタンスが代入されている。
$todoには、todoテーブル の 1レコード分のデータを持ったTodoインスタンスが代入されている。


## Todo作成機能

### Requestクラスのallメソッドは何をしているか
ユーザーから送信されたリクエストの全入力値を、連想配列として取得している。

### fillメソッドは何をしているか
引数に指定した連想配列を一括代入できる。

### $fillableは何のために設定しているか
一括代入には脆弱性があるため$fillableを定義して代入できる項目に制限をかけるために設定している。

### saveメソッドで実行しているSQLは何か
オブジェクトの状態をDBに保存するINSERT文を実行している。
SQL文：
INSERT INTO `todos` (`content`, `updated_at`, `created_at`) 
VALUES ('入力された値', '現在日時', '現在日時');

### redirect()->route()は何をしているか
指定したルート名のURLへリダイレクトさせるレスポンスを返している。


## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要。
マイグレーションファイルをGitで共有することで、開発者全員が同じテーブルを作成することができる。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()は、php artisan migrate（artisanコマンド）を実行した時。
down()は、php artisan migrate:rollback（rollbackコマンド）を実行した時。

### Seederクラスの役割は何か
データベースに初期データやテスト用データを投入すること。

### route関数の引数・返り値・使用するメリット
引数には、名前付きルート名
返り値には、そのルート名に対応するフルパスのURL文字列
メリットはURLの変更に強いこと。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extendsで継承する親Bladeを指定し、継承先の子Bladeの@section ~ @endsectionで囲われた部分を、親Bladeの@yieldの部分に挿入している。
引数に同じ文字列を指定することで、@sectionと@yieldを紐づけることができる。
bladeを分割するメリットは、共通の部分を別ファイルにまとめ、再利用可能にできること。

### @csrfは何のための記述か
送信フォームにワンタイムトークン（一時的な合言葉）を埋め込むための記述。
サーバー側でそのトークンをチェックすることで、悪意のある第三者によるなりすまし投稿を防いでいる。

### {{ }}とは何の省略系か
<?php echo e(...); ?> の省略形で、変数などを安全に画面に出力するための記述。