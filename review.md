# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
select文
### Todoモデルのallメソッドの返り値は何か
Illuminate\Database\Eloquent\Collection
### 配列の代わりにCollectionクラスを使用するメリットは
配列をそのまま扱うより、データを操作するための便利なメソッドがたくさん使える。
### view関数の第1・第2引数の指定と何をしているか
第一引数：どの画面に表示したいか。
第二引数：その画面にデータを渡す。
### index.blade.phpの$todos・$todoに代入されているものは何か
$todos = Todoテーブルから取得したTodoのデータすべて
オブジェクト型、入力したデータの値
$todo = foreachからひとつずつ取り出した値
オブジェクト型、

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
すべてのキーと値を取り出す
### fillメソッドは何をしているか
渡された配列のキーと値を、モデルの属性に一括でセットする
### $fillableは何のために設定しているか
fillメソッドで一括で入れる項目はを指定するため。
### saveメソッドで実行しているSQLは何か
INSERT または UPDATE
### redirect()->route()は何をしているか
URLへ、リダイレクト(転送)する
## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要
### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up:マイグレーションを適用する（テーブルを作る・変更する）
down:直前に実行したマイグレーションを取り消す
### Seederクラスの役割は何か
SQLを知らなくても、PHPコードでテーブル操作ができるため学習コストが不要
### route関数の引数・返り値・使用するメリット
URLを直接書かなくて済む
### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extends：親Bladeを指定
@section：子Bladeの内容を定義
@yield：その内容を表示
メリット：ページごとに違う部分だけ書くだけで済むので、コードの重複を減らせる。
### @csrfは何のための記述か
CSRFの対策。Laravelが自動的に CSRFトークンというランダムな値を生成して、フォームに埋め込んでくれる。
### {{ }}とは何の省略系か
phpのechoの省略形で、結果をHTMLエスケープして画面に表示するための記法