
# Laravel Lesson レビュー①
※一度自分で回答を作成しAIに整理してもらいました。

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か

**回答：**
`SELECT * FROM todos;`

---

### Todoモデルのallメソッドの返り値は何か

**回答：**
Eloquentの`Collection`（`Illuminate\Database\Eloquent\Collection`）。

Todoモデルのインスタンスが複数入ったコレクションとして返される。

---

### 配列の代わりにCollectionクラスを使用するメリットは

**回答：**
配列よりもデータを扱うための便利なメソッドが豊富で、可読性・保守性を高められる。

---

### view関数の第1・第2引数の指定と何をしているか

**回答：**
- 第1引数：表示するBladeファイルを指定
- 第2引数：Bladeファイルに渡すデータを指定

```php
view('todos.index', [
    'todos' => $todos
]);
````

ControllerからBladeファイルへデータを渡している。

---

### index.blade.phpの$todos・$todoに代入されているものは何か

**回答：**

* `$todos`：複数のTodoを持つCollection
* `$todo`：`$todos`から1件ずつ取り出したTodoモデル

---

## Todo作成機能

### Requestクラスのallメソッドは何をしているか

**回答：**
HTTPリクエストに含まれている入力データをすべて取得する。

---

### fillメソッドは何をしているか

**回答：**
取得したデータをモデルに一括代入する。

```php
$request->all()  // リクエストデータを取得
$todo->fill()     // モデルにデータを一括代入
$todo->save()     // DBに保存
```

---

### $fillableは何のために設定しているか

**回答：**
一括代入を許可するカラムを指定するため。

意図しないカラムへの一括代入を防ぐ役割がある。

---

### saveメソッドで実行しているSQLは何か

**回答：**

* 新規データの場合：`INSERT`
* 既存データの場合：`UPDATE`

---

### redirect()->route()は何をしているか

**回答：**
指定した名前付きルートへリダイレクトする。

---

## その他

### テーブル構成をマイグレーションファイルで管理するメリット

**回答：**
データベースの構造変更をコードとして管理できる。

* Gitで管理できる
* チームで共有できる
* 同じDB構造を再現できる
* 変更履歴を管理できる
* `rollback`で変更を戻せる

---

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか

**回答：**

* `up()` → `php artisan migrate`
* `down()` → `php artisan migrate:rollback`

`up()`でテーブルの作成・変更を行い、`down()`でその変更を元に戻す。

---

### Seederクラスの役割は何か

**回答：**
データベースに初期データやテストデータを挿入するためのクラス。

* Migration → テーブルの構造を作る
* Seeder → テーブルにデータを入れる

---

### route関数の引数・返り値・使用するメリット

**回答：**

* 第1引数：ルート名
* 第2引数：必要に応じて渡すパラメータ
* 返り値：URLの文字列

**メリット：**
URLを直接記述せず、ルート名からURLを生成できるため、可読性・保守性が高くなる。

---

### @extends・@section・@yieldの関係性とbladeを分割するメリット

**回答：**

* `@extends` → 親Bladeファイルを指定
* `@section` → 親に渡す内容を定義
* `@yield` → `@section`の内容を表示する場所

**Bladeを分割するメリット：**
共通部分を再利用でき、コードの重複を減らせるため、可読性・保守性が高くなる。

---

### @csrfは何のための記述か

**回答：**
CSRF攻撃を防ぐためのCSRFトークンをフォームに埋め込む。

---

### {{ }}とは何の省略系か

**回答：**
BladeでPHPの値をHTMLに出力するための記法。

```php
{{ $todo->content }}
```

PHPの値をエスケープしてHTMLに出力する。

```
```
