# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
todosテーブルのレコードを全件取得するといったSQL文
### Todoモデルのallメソッドの返り値は何か
Illuminate\Database\Eloquent\Collectionクラスのインスタンス
### 配列の代わりにCollectionクラスを使用するメリットは
viewメソッド
### view関数の第1・第2引数の指定と何をしているか
第一引数が表示したいファイル・第二引数が渡したいデータの連想配列
### index.blade.phpの$todos・$todoに代入されているものは何か
$todosには、Todoモデルの複数のレコード
$todoには、Todoモデル1件のレコードが代入されている
## Todo作成機能

### Requestクラスのallメソッドは何をしているか
POST通信でリクエストされたデータを全部取得する
### fillメソッドは何をしているか
連想配列で取得した値をTodoインスタンスの各プロパティに一括で代入している。
### $fillableは何のために設定しているか
なりすまし攻撃を防ぐため
### saveメソッドで実行しているSQLは何か
モデルにセットされたデータを自動判別し、保存するメソッド
### redirect()->route()は何をしているか
一覧ページへ画面遷移するために、リダイレクトレスポンスを返している
## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを知らなくてもphpコードでテーブル操作ができる
マイグレーションファイルをGitで共有することで開発者全員が同じテーブルを作成できる
### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
artisanコマンドを実行した時
### Seederクラスの役割は何か
テスト用のデータを登録するためのクラス
### route関数の引数・返り値・使用するメリット
引数はルート名
返り値はURLの文字列
そのルートに対するURLを生成することができる
### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extendsは、親を継承する
@sectionは、差し込む内容
@yieldは、差し込みの位置
親子関係

変更点があった際に親クラスのみ編集すれば全てに反映されるため、手間が省ける
### @csrfは何のための記述か
CSRF攻撃を防ぐためのセキュリティ記述
なりすまし攻撃を防ぐ
### {{ }}とは何の省略系か
phpの省略記法