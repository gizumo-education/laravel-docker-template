# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
@method('PUT') はLaravelのディレクティブ。
ブラウザで表示される際に、リクエストメソッドを擬装するためのhiddenタグを生成し、HTMLコードで出力される。
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
表示したい特定のレコードを識別するための、todosテーブルのidカラムの値

### findメソッドで実行しているSQLは何か
送られてきたルートパラメータのIDに一致し、かつ論理削除されていないレコードを取得するSQLが実行されている。
SQL文：
SELECT * FROM todos WHERE id = $id AND deleted_at IS NULL

### findメソッドで取得できる値は何か
送られてきたルートパラメータのIDに一致し、かつ論理削除されていないレコードのデータ（Todoモデルのインスタンス）。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
そのインスタンスが、既にデータベースに存在しているレコードかどうかを基準に判断している。
今回は、新規作成時はnew Todo() で作成された新しいインスタンスなので INSERT が実行され、
更新時は、find($id) などでデータベースから取得されたインスタンスなので UPDATE が実行される。

## Todo論理削除

### traitとclassの違いとは
classはインスタンス化することができるが、1つのクラスは1つの親クラスからしか機能を継承できないのに対し、
traitはインスタンス化することができないが、1つのクラスに複数のtraitを組み込むことができる。

### traitを使用するメリットとは
クラスの親子関係に縛られず、便利な機能として自由に使い回せること。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
インスタンス化される時。

### RequestクラスからFormRequestクラスに変更した理由
バリデーションの処理をコントローラーから切り離して、コードをスッキリさせるため。

### $errorsのhasメソッドの引数・返り値は何か
引数：バリデーションエラーを確認したいフォームの項目名（name属性の値）の文字列。
返り値：指定した項目にエラーが1つ以上存在する場合は true、エラーがない場合は false を返す。

### $errorsのfirstメソッドの引数・返り値は何か
引数：エラーメッセージを取得したいフォームの項目名（name属性の値）の文字列。
返り値：その項目に紐づく最初のエラーメッセージを文字列として返す。該当するエラーが存在しない場合は、空の文字列を返す。

### フレームワークとは何か
システム開発の土台となる共通の枠組みのこと。

### MVCはどういったアーキテクチャか
MVCは、アプリケーションの役割を 「Model（モデル）」「View（ビュー）」「Controller（コントローラー）」 という3つのコンポーネントに分離して設計するアーキテクチャ。
役割を分けることで、プログラムの再利用性や保守性を高めることを目的としている。

### ORMとは何か、またLaravelが使用しているORMは何か
ORMは、データベースのデータを、オブジェクトとして扱えるように変換する仕組みのこと。これにより、SQL文を直接書かなくても、プログラムのコードを使って直感的にデータの取得や保存ができる。
Laravelが使用しているORMは、Eloquent（エロクエント）。

### composer.json, composer.lockとは何か
composer.jsonは、プロジェクトが依存するパッケージ名と、許容するバージョンの範囲を定義する設定ファイル。開発者が手動で作成・編集し、インストールしたいライブラリの「設計図」の役割を果たす。
composer.lockは、実際にインストールされたパッケージの正確なバージョン情報を記録するファイル。composer install や update 時に自動生成され、チーム全体で全く同じバージョンのライブラリを使用することを保証する役割を持つ。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendor（ベンダー）ディレクトリ