# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
リクエストに含まれるTodoのidです。

### findメソッドで実行しているSQLは何か
SELECT * FROM テーブル名 WHERE id = id

### findメソッドで取得できる値は何か
引数で受け取ったIDに基づくレコードを取得します。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
UPDATEの場合、受け取ったidに基づくインスタンスかあれば切り替えている。

## Todo論理削除

### traitとclassの違いとは
traitはクラスと違いインスタンスができない。

### traitを使用するメリットとは
クラス自体は1つのクラスしか継承できないがトレイトを複数使うことができるのでクラス内で擬似的に複数のクラスを使うことができる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
ルートに対応するアクションのインスタンス生成されるとき。

### RequestクラスからFormRequestクラスに変更した理由
Requestクラスを継承してる上バリデーション実装に適したクラスであるため。

### $errorsのhasメソッドの引数・返り値は何か
入力の問題をチェックしたいキーを引数に指定。返り値は真偽を返す。

### $errorsのfirstメソッドの引数・返り値は何か
引数のキーに対応するエラーメッセージを一度だけ取得し文字列で返す。

### フレームワークとは何か
枠組みを指す。
多くの機能が予め用意されておりそこに適したコーディングをしていけば一定品質のアプリケーションができる。

### MVCはどういったアーキテクチャか
それぞれ役割を持っておりDB関連、UI関連、処理関連など分担している。

### ORMとは何か、またLaravelが使用しているORMは何か
オブジェクトとデータベースをつなぐ機能。
LaravelではEloquent ORMを使用してます。モデルを使いコードでデータベース操作が可能。

### composer.json, composer.lockとは何か
勉強不足で解りませんでした。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
composer自体はvenderに格納されてました。