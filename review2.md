# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
inputタグが生成される
 <input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
データベースのIDカラム

### findメソッドで実行しているSQLは何か
SELECT文

### findメソッドで取得できる値は何か
Todoモデルのインスタンス（編集対象のレコードの情報を持っている)
データベースのidカラムが$idの値と一致するレコードを取得している

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
モデルのインスタンスがデータベースに存在しているかどうかで切り替えている
インスタンスが存在しデータを取得している場合・・・UPDATE（データを更新する）
存在しない場合・・・INSERT（新規作成、追加がされる）

## Todo論理削除

### traitとclassの違いとは
trait…クラスにプロパティやメソッドを追加する、トレイト自体はインスタンス化することができない

class…メソッドやプロパティを定義しているクラス自体インスタンス化することができる

### traitを使用するメリットとは
1つのクラスに複数のトレイトを追加することができるので、クラス間でコードを共通化、再利用することができる

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
TodoControllerクラスが呼び出されたタイミングで実行される

### RequestクラスからFormRequestクラスに変更した理由
バリデーションを実装するため
FormRequestクラスはバリデーションを行うクラスの基本となるクラスであるため

### $errorsのhasメソッドの引数・返り値は何か
引数：content（フォームのname属性）
返り値：真偽値（true、false） エラーが発生しているかチェックをする

### $errorsのfirstメソッドの引数・返り値は何か
引数：content（フォームのname属性）
返り値：messages（）で定義したエラーメッセージ

### フレームワークとは何か
あらかじめディレクトリ構造が決まっている用意された枠組みのこと
※枠組みがあるので、コードの記述場所などがわかりやすい

### MVCはどういったアーキテクチャか
Model(モデル)
View(ビュー)
Controller(コントローラー)で構造されている

### ORMとは何か、またLaravelが使用しているORMは何か
ORM･･･プログラミング言語のclassとデータベースのテーブルをマッピングすることでSQLを直接操作せずにclassのメソッドで操作することができる

laravelではeloquentを用いている(これによりDBの操作を行うことが簡単にできる)

### composer.json, composer.lockとは何か
composer.json・・・インストールしたいパッケージの情報を記述する設定ファイル
composer installコマンドを実行することでパッケージの情報がインストールされる

composer.lock・・・インストールしたパッケージの内容が記載されているファイル
これによってグループでcomposer.lockファイルを共有すれば、同じパッケージ情報を共有することができる

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendorディレクトリ
