# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか

- <input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか

- ルート定義で指定したルートパラメータのid

### findメソッドで実行しているSQLは何か

- WHERE

### findメソッドで取得できる値は何か

- 引数に指定したidで取得したレコードの全データ

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか

- 指定されたidがテーブルに既に存在しているかを基準に切り替えている。

## Todo論理削除

### traitとclassの違いとは

- classはオブジェクトを作る事が目的
- traitは複数のクラスで共通する機能の追加が目的

### traitを使用するメリットとは

- 複数クラスで共通する関数をまとめることが可能。
- 再利用しやすい。
- 変更が一カ所で済む。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか

- Todoクラスがインスタンス化されたとき

### RequestクラスからFormRequestクラスに変更した理由

- バリデーションの設定をするため。

### $errorsのhasメソッドの引数・返り値は何か

- 引数: 入力欄のname属性
- 返り値: 真偽値
  
### $errorsのfirstメソッドの引数・返り値は何か

- 引数: 入力欄のname属性
- 返り値: string型（入力欄で最初に発生したエラーに関するエラーメッセージ）

### フレームワークとは何か

- 開発をする際にあらかじめ用意されている土台や枠組みのこと
  →Laravelそのものがフレームワーク

### MVCはどういったアーキテクチャか

- アプリケーションをModel（DB処理）、Controller（ModelとViewへの指示出し）、View（表示）の三つに分けたアーキテクチャ。

### ORMとは何か、またLaravelが使用しているORMは何か

- プログラミング言語のクラスとデータベースのテーブルを関連付ることで、SQLを直接操作せずにDBとのやり取りができるもの。
- 使用しているORM: Eloquent

### composer.json, composer.lockとは何か

- composer.json: パッケージやバージョンなどの依存関係を管理する設定ファイル
- composer.lock: インストールしたバッケージの状態やバージョンなどを定義するファイル


### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか

- vendorディレクトリ

