# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
・input type="hidden" name="method" value="PUT"というinputタグを生成して出力できる。
### findメソッドの引数に指定しているIDは何のIDか
・詳細表示するためのコンテントのID。
### findメソッドで実行しているSQLは何か
・select * from todos where id = ◯ limit ◯; というselect文が自動的に実行される。
### findメソッドで取得できる値は何か
・引数の値を条件として、取得したレコードを取得できる。
### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
・id（主キー）があるかないかで切り替えを行っている。idが無い場合はINSERTを実行し、idが既にある場合はUPDATEを実行する。
## Todo論理削除

### traitとclassの違いとは
・クラスの継承とは異なり、1つのクラスに複数のトレイトを追加することができる。
・トレイト自体はインスタンス化できない。
### traitを使用するメリットとは
・クラスの継承関係に縛られず、コードの共通化・再利用が可能な点。
## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
・controllerが呼び出される直前に自動で実行される。
### RequestクラスからFormRequestクラスに変更した理由
・FormRequestクラスは、バリデーションを行うクラスの基本となるクラスだから。（バリデーションとデータ取得を同時に行うことができるから。）
### $errorsのhasメソッドの引数・返り値は何か
・hasメソッドの引数は「入力欄のname属性」となり、入力欄でバリデーションエラーが発生しているか判定する。
・hasメソッドの返り値はtrueまたはfalseとなり、指定したキーが存在するか判定する。
### $errorsのfirstメソッドの引数・返り値は何か
・firstメソッドの引数は「入力欄のname属性」となり、入力欄で最初に発生したエラーメッセージを出力している。
・firstメソッドの返り値はモデルインスタンスまたはnullとなる。
### フレームワークとは何か
・Webアプリやシステム開発を効率化するための枠組みのこと。
### MVCはどういったアーキテクチャか
・開発効率を高めるために役割分担をさせる設計方法。
・M（Model)は、データベースとのやり取りを担当している。
・V（View)は、ユーザーに表示する画面の生成を担当している。
・C（Controller)は、ユーザーからの入力に基づき、モデルとビューの仲介を担当している。
### ORMとは何か、またLaravelが使用しているORMは何か
・ORM（オブジェクト・リレーショナル・マッピング）は、プログラミング言語のクラスとデータベースのテーブルを関連付けすることで、SQLを直接操作することなくデータベースと関連付けされたクラスのメソッドを用いてDBのやり取りを行うことができるといった概念。
・ Laravelでは、EloquentというORMを使用している。
### composer.json, composer.lockとは何か
・composer.jsonとは、プロジェクトが依存する外部ライブラリやバージョン情報を記述するJSON形式の設定ファイル。
・composer.lockとは、プロジェクトで使用する全ライブラリの正確なバージョンを記録するファイル。
### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
・vendorディレクトリに格納される。