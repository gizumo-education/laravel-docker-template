# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
参照しているデータベースのキー

### findメソッドで実行しているSQLは何か
SELECT * FROM todos WHERE id = (キーの値) LIMIT 1;

### findメソッドで取得できる値は何か
指定したキーに対応するレコードを持つモデルのインスタンス

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
キーの値がセットされている場合はUPDATE、そうでない場合はINSERT

## Todo論理削除

### traitとclassの違いとは
traitは、classの継承とは異なり1つのclassに複数のtraitを追加することができ、trait自体はインスタンス化できない

### traitを使用するメリットとは
複数のクラスで共通の機能をまとめて管理できるので、修正が一箇所で済みやすくなり、コードの可読性や保守性が向上する

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
コントローラーのメソッドが呼ばれる直前の、Laravelによる自動でのインスタンスの生成時

### RequestクラスからFormRequestクラスに変更した理由
FormRequestクラスは認可やバリデーションの機能が組み込まれており、これらを簡単かつ分かりやすく実装できるため

### $errorsのhasメソッドの引数・返り値は何か

引数：バリデーションエラーを確認したいフォームの入力項目名
返り値：その項目にエラーがあればtrue、なければfalse

（$errors->has()は、フォームにおいてエラーメッセージの有無をチェックするためのメソッド）

### $errorsのfirstメソッドの引数・返り値は何か

引数：バリデーションエラーを確認したいフォームの入力項目名
返り値：関連する最初のエラーメッセージ（文字列）、なければnull

### フレームワークとは何か
開発を効率よく進めるために、よく使う機能をまとめて提供する枠組み

### MVCはどういったアーキテクチャか
ソフトウェアを「モデル（DBとのやり取りを司る）」、「ビュー（ユーザに表示する画面の生成を司る）」、「コントローラ（ユーザからの送信データに基づき、Model・Viewの制御を司る）」の3つの役割に分けて設計するもの

### ORMとは何か、またLaravelが使用しているORMは何か
・プログラミング言語のクラスとデータベースのテーブルを関連付け（マッピング）し、SQLを直接書かずに、クラスのメソッドを通じてデータベース操作を行う技法
・Laravelでは「Eloquent ORM」が採用されている

### composer.json, composer.lockとは何か
composer.json：プロジェクトが依存するパッケージを定義するファイル
（composer installコマンドでここに書かれたパッケージをインストールする）
composer.lock：実際にインストールされたパッケージの正確なバージョンや依存関係を記録するファイル

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendorディレクトリに格納される